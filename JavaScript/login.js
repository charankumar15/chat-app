document.getElementById('loginForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // You can add login functionality here, e.g., authentication
    window.location.href = 'chat.html'; // Redirect to the chat page
});
